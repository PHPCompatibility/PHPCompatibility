<?php
use Foobar as Foo, BarFoo as Baz;
use Foobar as Foo, BarFoo as abstract;
use Foobar as Foo, BarFoo as and;
use Foobar as Foo, BarFoo as array;
use Foobar as Foo, BarFoo as as;
use Foobar as Foo, BarFoo as break;
use Foobar as Foo, BarFoo as callable;
use Foobar as Foo, BarFoo as case;
use Foobar as Foo, BarFoo as catch;
use Foobar as Foo, BarFoo as class;
use Foobar as Foo, BarFoo as clone;
use Foobar as Foo, BarFoo as const;
use Foobar as Foo, BarFoo as continue;
use Foobar as Foo, BarFoo as declare;
use Foobar as Foo, BarFoo as default;
use Foobar as Foo, BarFoo as die;
use Foobar as Foo, BarFoo as do;
use Foobar as Foo, BarFoo as echo;
use Foobar as Foo, BarFoo as else;
use Foobar as Foo, BarFoo as elseif;
use Foobar as Foo, BarFoo as empty;
use Foobar as Foo, BarFoo as enddeclare;
use Foobar as Foo, BarFoo as endfor;
use Foobar as Foo, BarFoo as endforeach;
use Foobar as Foo, BarFoo as endif;
use Foobar as Foo, BarFoo as endswitch;
use Foobar as Foo, BarFoo as endwhile;
use Foobar as Foo, BarFoo as eval;
use Foobar as Foo, BarFoo as exit;
use Foobar as Foo, BarFoo as extends;
use Foobar as Foo, BarFoo as final;
use Foobar as Foo, BarFoo as finally;
use Foobar as Foo, BarFoo as for;
use Foobar as Foo, BarFoo as foreach;
use Foobar as Foo, BarFoo as function;
use Foobar as Foo, BarFoo as global;
use Foobar as Foo, BarFoo as goto;
use Foobar as Foo, BarFoo as if;
use Foobar as Foo, BarFoo as implements;
use Foobar as Foo, BarFoo as include;
use Foobar as Foo, BarFoo as include_once;
use Foobar as Foo, BarFoo as instanceof;
use Foobar as Foo, BarFoo as insteadof;
use Foobar as Foo, BarFoo as interface;
use Foobar as Foo, BarFoo as isset;
use Foobar as Foo, BarFoo as list;
use Foobar as Foo, BarFoo as namespace;
use Foobar as Foo, BarFoo as new;
use Foobar as Foo, BarFoo as or;
use Foobar as Foo, BarFoo as print;
use Foobar as Foo, BarFoo as private;
use Foobar as Foo, BarFoo as protected;
use Foobar as Foo, BarFoo as public;
use Foobar as Foo, BarFoo as require;
use Foobar as Foo, BarFoo as require_once;
use Foobar as Foo, BarFoo as return;
use Foobar as Foo, BarFoo as static;
use Foobar as Foo, BarFoo as switch;
use Foobar as Foo, BarFoo as throw;
use Foobar as Foo, BarFoo as trait;
use Foobar as Foo, BarFoo as try;
use Foobar as Foo, BarFoo as unset;
use Foobar as Foo, BarFoo as use;
use Foobar as Foo, BarFoo as var;
use Foobar as Foo, BarFoo as while;
use Foobar as Foo, BarFoo as xor;
use Foobar as Foo, BarFoo as yield;
use Foobar as Foo, BarFoo as __CLASS__;
use Foobar as Foo, BarFoo as __DIR__;
use Foobar as Foo, BarFoo as __FILE__;
use Foobar as Foo, BarFoo as __FUNCTION__;
use Foobar as Foo, BarFoo as __METHOD__;
use Foobar as Foo, BarFoo as __NAMESPACE__;
